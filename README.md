由于我目前只有V相和W相的电压检测，我想要在原有的在进入SynInitPosDetect这个函数之前读取电流之外再读取V,W两相的电压，用来在我最后计算电感时能通过两次电流检测时采到的电压值计算出平均电压值带入计算。现在请根据代码中原有的ADC，PTIMER配置为V,W电流检测更新配置，原有的ADC0都是软件触发，现在你需要把ADC0也配置成硬件触发，让PWM作为PTIMER1的触发源之外还触发PTIMER0用来延时触发ADC0，因为V,W相的电压检测分别是ADC0的通道4和通道1，注意PTIMER中的延时，因为我在进ADC1完成中断在其中调用SynInitPosDetect这个函数之前要去读取ADC0中采集到的电压值，因此此时ADC0的本轮采集需要是已经完成的状态。不要修改原有的代码，你要另外新建一个工程来完成这项工作。
以上内容与本次更新无关
